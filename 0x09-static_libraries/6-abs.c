#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @x: function parameter
 * Return: -x or x;
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	return (-x);
}
